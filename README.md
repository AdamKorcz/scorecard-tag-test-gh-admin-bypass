# scorecard-tag-test-gh-admin-bypass

Repository for testing Scorecard tag protection check.

This repository has 1 ruleset targetting `refs/tags/v*` with "Restrict deletions", "Block force pushes", "Restrict updates", "Restrict creations". Admins can bypass.
